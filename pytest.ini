[pytest]
testpaths = tests
pythonpath = .
addopts = -q --cov=app --cov-report=term-missing:skip-covered --cov-fail-under=55
markers =
    api: machine API / PDM integration surface
    web: browser session / CSRF / wizard HTML
    lab: destructive live lab smoke (not run by default)
